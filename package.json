{
  "name": "todoapp",
  "displayName": "todoApp",
  "version": "1.0.0",
  "description": "A sample todo app",
  "scripts": {
    "dev": "browser-sync start -s www/ --watch --port 8080 --ui-port 8081"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7"
  }
}
